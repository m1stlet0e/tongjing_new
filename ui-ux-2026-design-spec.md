# TongJing 2026 移动端 UI/UX 设计规范（五大 Tab）

## 1. 目标与原则

本规范用于统一 TongJing App 的视觉与交互，核心目标：
- 强化“摄影内容第一优先”的视觉体验。
- 让“参数学习 + 机位执行 + 社区互动”形成完整闭环。
- 引入 AI 辅助，但不抢用户主操作路径。

设计原则：
- 内容优先：照片是第一视觉层，功能入口是第二层。
- 信息分层：主信息（图、标题）> 次信息（参数、距离）> 扩展信息（说明、推荐）。
- 一屏一任务：每个 Tab 的首屏明确一个核心任务。
- 可执行导向：所有浏览动作都能导向“加入计划”或“去打卡”。

---

## 2. 全局规范

## 2.1 导航结构
- 底部五 Tab：`首页` / `机位地图` / `发布` / `计划与挑战` / `我的`
- 中央发布按钮为强调操作（主行动作）

## 2.2 视觉风格
- 主色：克莱因蓝 `#002FA7`
- 背景：浅灰白 `#FAFAFA`
- 卡片：白底 + 轻阴影 + 12~16 圆角
- 字体层级：
  - 页面标题 20~24
  - 卡片标题 14~16
  - 参数信息 11~13

## 2.3 交互反馈
- 所有异步请求必须有可见反馈（Skeleton/Loading/Toast）
- 所有失败状态必须有可恢复动作（重试）
- 所有“可执行动作”需要明确下一步（如收藏后跳转计划）

---

## 3. Tab 1 首页（Home）

## 3.1 页面结构
1) 顶部导航区：
- 搜索栏（支持关键词与 AI 标签）
- 分类 Tab：关注 / 推荐 / 最新
- 右侧高级筛选入口（漏斗）

2) 主体内容区：
- 双列瀑布流（Staggered Grid）
- 卡片包含：
  - 图片预览（主视觉）
  - 第一行：标题 + 作者
  - 第二行：参数条（相机 / 焦段 / 光圈）+ 距离信息

3) 详情页（由首页点击进入）：
- 上半屏大图（支持缩放）
- 下半屏信息面板：
  - 作者与文案
  - EXIF 参数卡
  - 机位迷你地图
  - 底部操作：点赞 / 评论 / 分享 / 加入计划

## 3.2 交互要求
- 下拉刷新 + 无限加载
- 切换 Tab 时保留筛选状态
- 筛选支持组合条件（品牌 / 镜头 / 光圈 / 场景）

---

## 4. Tab 2 机位地图（Photo Map）

## 4.1 页面结构
1) 全屏地图层：
- 默认定位当前位置
- 机位点使用“照片气泡”样式

2) 顶部悬浮控件：
- AI 环境预测条（天气与拍摄窗口）
- 搜索框（地标、城市、机位关键词）
- 快速筛选（半径、热度）

3) 底部抽屉：
- 默认 1/3 高度显示热门机位
- 上滑展开全屏列表
- 支持按距离/热度排序

## 4.2 交互要求
- 点击热门机位 -> 地图自动飞到该点 + 刷新附近作品
- 搜索关键词同步过滤地图点与列表
- 地图拖动结束自动刷新当前视野结果

---

## 5. Tab 3 发布（Publish）

## 5.1 页面结构
步骤一：选图与 AI 解析
- 本地相册选择后进入“解析中”状态（EXIF + 场景识别）

步骤二：编辑与发布（上下布局）
- 上半区：图片预览 + AI 工具入口（消除/裁剪/滤镜）
- 中部卡片：
  - 文案输入 + AI 帮写按钮
  - EXIF 参数卡（可编辑）
  - 位置打卡卡（可拖拽微调）
  - 标签卡（场景/风格/设备）
- 底部操作：存草稿 / 发布

## 5.2 交互要求
- EXIF 自动填充，支持手动修正
- 标签支持 AI 推荐、一键采纳与手动删除
- 发布成功后提供“继续发布/返回首页”双路径

---

## 6. Tab 4 计划与挑战（Plan & Challenge）

## 6.1 页面结构
顶部双 Tab：我的计划 / 同款挑战

我的计划：
- 待办卡片：目标图、机位距离、参数摘要
- AI 建议：天气/时段建议
- 动作按钮：去打卡（跳地图导航）

同款挑战：
- 顶部主题 Banner（官方活动）
- 挑战列表（热度排序）
- 进入挑战详情可查看参与作品网格

## 6.2 交互要求
- 支持计划执行状态切换（未执行/已执行）
- 支持从详情页一键加入计划并即时可见

---

## 7. Tab 5 我的（Profile）

## 7.1 页面结构
1) 顶部名片：
- 背景图 + 头像 + 昵称 + 简介
- 指标：获赞 / 粉丝 / 关注 / 已打卡机位

2) 装备库（核心特色区）：
- 横向滑动卡片展示机身/镜头
- 强化“摄影身份感”

3) 底部内容 Tab：
- 作品（3 列网格）
- 足迹（点亮地图）
- 收藏（灵感文件夹）

## 7.2 交互要求
- 下拉刷新资料与作品
- 快捷入口直达设置、编辑资料、我的计划

---

## 8. 开发落地映射（本次改版范围）

本阶段优先落地：
- 首页：搜索 + 顶部分类 + 参数卡片层级优化
- 机位地图：AI 预测条 + 搜索/筛选 + 热门机位联动
- 发布：编辑页面模块化卡片 + AI 标签推荐入口
- 计划与挑战：双 Tab + 待办卡片执行状态
- 我的：顶部名片 + 装备库横向卡片 + 内容分区

后续阶段继续增强：
- 瀑布流异形卡片动画
- 详情页底部可拖拽面板
- AI 文案与天气预测真实接口接入
